# Patterns

Stable project patterns go here.

## Current Patterns

- Keep OpenCode as the coding-agent runtime.
- Keep DevTools Radar Local API as the model backend.
- Use small, reviewable changes.
- Prefer serial workflow before parallel agents.
- Record reusable workflows as commands or skills.